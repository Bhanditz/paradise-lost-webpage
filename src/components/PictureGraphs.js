import React from 'react';
import BarCode from './BarCode';
import PersDiagram from './PersDiagram';
import DistMatrix from './DistMatrix';

// Data.  Import only one!

const styles = {
  width: 800,
  height: 800,
  margin: {
    sides: 40,
    top: 40,
    bottom: 40,
  },
};

function PictureGraphs() {
  return <svg width />;
}

export default PictureGraphs;
